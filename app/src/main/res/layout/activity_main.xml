<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!--Este contenedor era el inicial antes de incluir la guia con RelativeLayout-->

    <!--Incluimos layout de la guia en main layout
    al estar más abajo en el layout.xml se superpone sobre el main layout-->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:layout_marginEnd="0dp"
        android:layout_marginBottom="0dp">

        <!-- NavHostFragment para manejar la navegación entre fragmentos -->
        <androidx.fragment.app.FragmentContainerView
            android:id="@+id/navHostFragment"
            android:name="androidx.navigation.fragment.NavHostFragment"
            android:layout_width="0dp"
            android:layout_height="0dp"
            app:defaultNavHost="true"
            app:layout_constraintBottom_toTopOf="@id/navView"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:navGraph="@navigation/nav_graph" />

        <!-- BottomNavigationView para la navegación inferior -->
        <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/navView"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:menu="@menu/bottom_nav_menu" />

    </androidx.constraintlayout.widget.ConstraintLayout>

    <include
        android:id="@+id/guideBienvenidaLayout"
        layout="@layout/guide_bienvenida"
        android:visibility="gone" />

    <include
        android:id="@+id/guidePersonajesLayout"
        layout="@layout/guide_personajes"
        android:visibility="gone" />

    <include
        android:id="@+id/guideMundosLayout"
        layout="@layout/guide_mundos"
        android:visibility="gone" />

    <include
        android:id="@+id/guideColeccionablesLayout"
        layout="@layout/guide_coleccionables"
        android:visibility="gone" />

    <include
        android:id="@+id/guideInfoLayout"
        layout="@layout/guide_info"
        android:visibility="gone" />

    <include
        android:id="@+id/guideResumenLayout"
        layout="@layout/guide_resumen"
        android:visibility="gone" />

</RelativeLayout>